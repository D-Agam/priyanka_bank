<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Account Summary</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f0f0f0;
            background: linear-gradient(to bottom, #f5f6f8, #98ceed);
        }
        .header {
            background-color: #007bff;
            color: #fff;
            padding: 10px;
            text-align: center;
            font-size: 24px;
        }
        .container {
            max-width: 600px;
            margin: 20px auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .form-group {
            margin-bottom: 20px;
        }
        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }
        select {
            width: 100%;
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }button{
            background-color: #7198ea;
            border: none;
            padding: 10px 15px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s ease-in-out;
        }
        input[type="submit"] {
            background-color: #3b823b;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
        }
        input[type="submit"]:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <marquee class="header" behavior="scroll" direction="left">Account Summary</marquee>
    <div class="container">
        <h2>Filter Account Summary</h2>  
        <form action="/view_summary" method="post">
            <div class="form-group">
                <label for="employeeId">Bank Employee ID</label>
                <input type="text" id="employeeId" name="employeeId" required>
            </div>
            <div class="form-group">
                <label for="username">Username</label>
                <input type="text" id="username" name="username" required>
            </div>
            <div class="form-group">
                <label for="accountId">Account ID</label>
                <input type="text" id="accountId" name="accountId" required>
            </div>
            <div class="form-group">
                <label for="filterType">Filter by:</label>
                <select id="filterType" name="filterType" required>
                    <option value="credit">Credit History</option>
                    <option value="debit">Debit History</option>
                    <option value="meeting">Meeting History</option>
                    <option value="5t">Top 5 transactions</option>
                    <option value="meeting">Top 5 Credit transactions</option>
                    <option value="meeting">Top 5 Debit transactions</option>
                </select>
            </div>
            <div class="form-group">
                <label for="timeFilter">Time Filter:</label>
                <select id="timeFilter" name="timeFilter" required>
                    <option value="1">Last 1 Days</option>
                    <option value="5">Last 5 Days</option>
                    <option value="10">Last 10 Days</option>
                </select>
            </div>
            <button type="submit">
                Apply Filter
            </button>
        </form>
    </div>

</body>
</html>
